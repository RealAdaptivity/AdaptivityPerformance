# Deprecated — Capacitor iOS artifact from the website is not the store app.
# Use EAS on AdaptivityTechApp / AdaptivityCustomerApp.

name: Build Mechanic iOS App Artifact (deprecated)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Use Expo apps instead
        run: |
          echo "::warning::Capacitor iOS builds from AdaptivityPerformance are deprecated."
          echo "Use https://github.com/RealAdaptivity/AdaptivityTechApp (EAS) for the tech app."
          exit 1
