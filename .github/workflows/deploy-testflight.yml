# Deprecated — do not use for store / TestFlight builds
#
# Native apps ship from the Expo repos via EAS:
#   https://github.com/RealAdaptivity/AdaptivityTechApp
#   https://github.com/RealAdaptivity/AdaptivityCustomerApp
#
# This workflow previously wrapped the marketing website in Capacitor.
# It is disabled so CI does not waste minutes or confuse the launch path.

name: Mechanic Capacitor iOS / TestFlight (deprecated)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Use Expo apps instead
        run: |
          echo "::warning::Capacitor TestFlight from this website repo is deprecated."
          echo "Build AdaptivityTechApp / AdaptivityCustomerApp with EAS instead."
          exit 1
