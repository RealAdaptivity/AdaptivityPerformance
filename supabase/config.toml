# Supabase CLI — AdaptivityPerformance (qqyairzymqpkbfxobztx)
# https://supabase.com/docs/guides/cli/config

project_id = "qqyairzymqpkbfxobztx"

[api]
enabled = true

[db]
major_version = 15

[functions]
# Per-function JWT: create-booking-request is deployed with --no-verify-jwt so
# guests can book without an account. Everything else verifies a JWT.
