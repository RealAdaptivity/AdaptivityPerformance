-- Applied historically on remote as stripe_payments.
-- Kept as a no-op stub so local migration history matches supabase_migrations.schema_migrations.
-- Schema for payments already exists via later migrations / initial platform.
SELECT 1;
